# WHY: Example inventory — copy to inventory.ini (gitignored) with the real EC2 host.
# ansible-playbook -i inventory.ini ansible/portfolio-server-setup.yml

[portfolio]
portfolio-ec2 ansible_host=<EC2_PUBLIC_IP_OR_DNS> ansible_user=ubuntu

[portfolio:vars]
ansible_python_interpreter=/usr/bin/python3
# ansible_ssh_private_key_file=~/.ssh/<SUA_CHAVE>.pem
